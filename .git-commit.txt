feat: support Unsloth gradient checkpointing mode

- unsloth_sft/train_sft.py:
  - Accept `model.gradient_checkpointing` config as:
    - true/false: standard HF gradient checkpointing on/off
    - "unsloth": pass through to FastLanguageModel.from_pretrained(use_gradient_checkpointing="unsloth") for Unsloth’s optimized GC.
  - Avoid double-enabling: skip manual model.gradient_checkpointing_enable() when using Unsloth mode.

This allows using Unsloth’s custom checkpointing behavior when requested in YAML.
