feat(eval): add chat-template prompting to MATH-500 eval

- scripts/eval_math500.py
  - New flags: --use_chat_template, --system_prompt
  - Chat mode builds [system, user] messages and tokenizes via tokenizer.apply_chat_template with add_generation_prompt
  - Retains plain prompt mode as default; both use attention_mask-based slicing

This enables fair evaluation of chat-instruct models like Gemma-3-Instruct.
