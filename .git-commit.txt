chore(logging): suppress default Trainer prints; color keys same as values

- unsloth_sft/train_sft.py:
  - Use transformers.utils.logging.set_verbosity_error() to silence default info dict logs (avoid dual prints).
  - In RichLogCallback, color the metric names the same as their values.
  - Fix minor formatting.
