chore(logging): revert to default Unsloth/Trainer step logging

- Remove RichLogCallback and related imports; stop suppressing HF logs.
- Trainer now uses default logging behavior (dict-style step logs), per request.
