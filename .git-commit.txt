feat(trainer): optional JSON→JSONL conversion before preprocessing

- unsloth_sft/train_sft.py:
  - Add data.convert_to_jsonl flag. When true, detect if the training file is a JSON array/dict; if so, convert to JSONL (one object per line) and update train_jsonl_path in-memory before pretokenization.
  - Optional config knobs: data.convert_to_jsonl_include_keys (list) and data.convert_to_jsonl_renames (mapping from old->new).
- configs/sft_unsloth.yaml: document the new flags (default false) with commented examples.

This allows large JSON datasets like MetaMathQA to be consumed directly without manual preprocessing.
