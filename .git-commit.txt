chore(tqdm): ensure progress bar is enabled

- unsloth_sft/train_sft.py: explicitly set TrainingArguments.disable_tqdm=False and logging_strategy='steps' so the default tqdm bar (speed/progress/steps) is shown.
