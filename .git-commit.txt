chore: rich-colored step logs and decimal LR; ignore swap files

- unsloth_sft/train_sft.py: add RichLogCallback to pretty-print training step logs with colorblind-friendly value colors (loss=cyan, grad_norm=green3, learning_rate=orange3, epoch=magenta). Format learning_rate as fixed decimal (non-scientific). Also switch Trainer to processing_class, set PYTORCH_CUDA_ALLOC_CONF, add optional num_train_epochs, and robust collator remains.
- .gitignore: ignore editor swap files (*.swp, *.swo, *~), .DS_Store, and stray .install.sh.swp.

This removes noisy warnings and improves readability of step logs while ensuring LR is printed as decimal.
