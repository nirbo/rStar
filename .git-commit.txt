fix(config-parse): robust numeric parsing with helpful errors

- unsloth_sft/train_sft.py: add _as_int/_as_float helpers and use them for all numeric TrainingArguments fields (batch sizes, grad_accum, steps, logging/eval/save cadence, dataloader workers, seed, optimizer params). This produces clear, actionable errors like:
  "Config training.gradient_accumulation_steps must be an integer. Got '6i'."
- Prevents cryptic ValueError and guides fixing configs/sft_unsloth.yaml.
