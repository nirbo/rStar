feat(trainer): auto-resume from latest checkpoint if present

- unsloth_sft/train_sft.py: detect last checkpoint in output_dir via transformers.get_last_checkpoint and pass resume_from_checkpoint=path to Trainer.train(). Starts fresh if no checkpoints found.
