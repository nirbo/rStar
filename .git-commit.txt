chore(eval,merge): silence deprecations and generation warnings

- scripts/eval_math500.py
  - Switch HF load to `dtype=` (from deprecated `torch_dtype`)
  - Only pass sampling flags when do_sample=True (avoid warnings in greedy mode)
  - Slice responses using per-sample prompt length via attention_mask sum
- unsloth_sft/merge_lora.py
  - Switch HF load to `dtype=` per new API

Minor quality-of-life improvements; functionality unchanged.
